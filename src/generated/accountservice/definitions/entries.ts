import { Entry } from "./entry";

/**
 * Entries
 * @targetNSAlias `tns`
 * @targetNamespace `http://24sevenOffice.com/webservices`
 */
export interface Entries {
    /** Entry[] */
    Entry?: Array<Entry>;
}
